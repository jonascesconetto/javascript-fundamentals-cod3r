/**
 * Unário = um operador
 * Binário = dois Operadores
 * Ternário = tres operadores
 */

// 1 - expressão nota >= 7 2 (se verdadeiro) - "Aprovado" 3 (se falso) - "reprovado"
const resultado = nota => nota >= 7 ? 'Aprovado' : 'Reprovado' 

console.log(resultado(7.1))
console.log(resultado(3.1))